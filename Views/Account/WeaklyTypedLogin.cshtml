<h3>Weakly Typed Login Form</h3>
@using(Html.BeginForm("LoginPost", "Account", FormMethod.Post))
{
    <div>Username: @Html.TextBox("Username")</div>
    <div>Password: @Html.Password("Password")</div>
    <div><input type="submit" value="Login" /></div>
}